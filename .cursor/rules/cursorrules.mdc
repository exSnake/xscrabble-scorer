---
description: 
globs: 
alwaysApply: true
---

# Your rule content

Non avviare mai con npm run dev alla fine della scrittura del codice, il progetto è gia avviato in un terminale
Puoi accedere a qualsiasi file di [App.vue](mdc:src/App.vue) [ScorerView.vue](mdc:src/views/ScorerView.vue) [ScorerAddWord.vue](mdc:src/components/ScorerAddWord.vue) [PlayerDetails.vue](mdc:src/components/PlayerDetails.vue) [TheWelcome.vue](mdc:src/components/TheWelcome.vue) [WelcomeItem.vue](mdc:src/components/WelcomeItem.vue) [index.js](mdc:src/router/index.js) e altri...
Ogni volta che termini una modifica lancia `npm run lint` e LEGGI L'OUTPUT!!! IMPORTANTE, controlla che non ci siano errori, se ci sono, risolvili!
Usa Tailwind per lo styling, se devi creare stili personalizzati non presenti, chiedi!
Usa Lucide per le icone
Cerca di essere quanto più modulare possibile, se un componente fa troppe cose dividilo, usa le best pracices di vue:
- Preferisci il Composition API all'Options Api
- Always use inside v-for
- Use kebab-case for events
- Declare props with camelCase and use kebab-case in templates
- Data should always return a function
- Don’t use v-if with v-for elements
- Validate your props with good definitions
- Use PascalCase for components
- Base components should be prefixed accordingly
- Components declared and used ONCE should have the prefix “The”
- Stay consistent with your directive shorthand A common technique among
- Don’t call a method on created AND watch
- Template expressions should only have basic JavaScript expressions